#!/bin/sh
# The JS test suite runs before push (~2 min). It's slower than the
# pre-commit checks, so it gates push rather than every commit.
# Python/Go/coverage stay in CI. Skip with `git push --no-verify`.
set -e
npm test
